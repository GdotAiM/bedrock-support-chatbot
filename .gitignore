__pycache__/
*.pyc
.env
.venv/
eval/run_result.jsonl
eval/bedrock_eval_dataset.jsonl
logins.txt
*.xlsx
*.key
*.pem
*.p12

# Internal deployment scripts (contain account-specific configs)
cloudformation*.yaml
deployed-eval.ps1
verify-and-eval.ps1
create_eval_job.py
playwright-rubric-screenshots.js

# Internal architecture docs (contain full ARNs and account details)
AUDIT_REPORT.md
SYSTEM_AUDIT.md
AGENTCORE_GATEWAY_INTEGRATION.md
PROJECT_PLAN.md
MIGRATE_TO_BEDROCK_FLOWS.md

# Flow definitions (internal infrastructure)
bedrock/flow_definition*.json

# Credentials and sensitive data
logins.txt
*.xlsx
*.key
*.pem
*.p12
.env

# Eval results (may contain PII)
eval/bedrock_eval_dataset.jsonl
EVAL_SUMMARY.json

# Screenshots / rubric evidence (included in submission)
# No exclusion — these are part of the deliverables

# Eval result variants (keep only v4)
eval/run_result_deployed.jsonl
eval/run_result_deployed_final.jsonl
eval/run_result_deployed_v3.jsonl
eval/run_result_deployed_v3_cleaned.jsonl
eval/bedrock_eval_dataset.jsonl
eval/bedrock_eval_dataset_v2.jsonl
eval/bedrock_eval_dataset_v3.jsonl
